package org.example.vlume.pages.panels.toppanel.profile.settings.profileparts;

import org.example.vlume.BasePage;
import org.openqa.selenium.WebDriver;

public abstract class ProfilePartDiv extends BasePage {

    protected ProfilePartDiv(WebDriver driver) {
        super(driver);
    }
}
